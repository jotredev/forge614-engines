{
  "name": "forge614-engines",
  "private": true,
  "version": "1.6.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "verify:docs": "bun scripts/verify-documentation.mjs",
    "build": "bun build ./src/interfaces/cli/main.ts --compile --outfile dist/forge614-engines",
    "release:bundle": "bun scripts/release-bundle.mjs",
    "release:cut": "bun scripts/release-cut.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "bun-types": "^1.3.8"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "jsonc-parser": "^3.3.1",
    "smol-toml": "^1.8.0"
  }
}
